{
  "name": "tragdev",
  "version": "1.0.0",
  "scripts": {
    "clean": "rm -rf dist/* && mkdir -p dist",
    "dev": "npm run clean && eleventy --serve --watch",
    "build": "npm run clean && eleventy",
    "debug": "DEBUG=* eleventy"
  },
  "dependencies": {
    "@11ty/eleventy": "^2.0.1",
    "@picocss/pico": "^1.5.10"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/chris-trag/tragdev.git"
  },
  "license": "ISC",
  "devDependencies": {
    "markdown-it-attrs": "^4.3.1",
    "markdown-it-mark": "^4.0.0"
  }
}
